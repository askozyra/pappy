<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Core.Views.Desktop.Components.Window.Titlebar">
    <Grid Style="{StaticResource WindowTitlebar}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <HorizontalStackLayout Grid.Column="0">
            <Image Style="{StaticResource AppIcon}"/>
            <Label Text="[placeholder]"/>
        </HorizontalStackLayout>
        <HorizontalStackLayout Grid.Column="2">
            <Button Margin="1" Text=".." Clicked="MinimizeWindow"/>
            <Button Margin="1" Text="‹›" Clicked="MaximizeRestoreWindow"/>
            <Button Margin="1" Text="×" Clicked="CloseWindow"/>
        </HorizontalStackLayout>
    </Grid>
</ContentView>
